{
  "name": "basedupon-terrain",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone development harness for the fly-through terrain component.",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "nuxt": "^4.4.8",
    "three": "^0.185.1"
  }
}
